# 项目简介
这个项目名叫【丝月工坊】，是我的个人本地工具站，核心目的是将日常高频使用的小功能集中到一个地方，减少App之间的反复切换。

# 功能介绍
目前大概以下功能：
- 收藏馆：收藏我喜欢的动漫，游戏，音乐，电影等等。接入Bangumi以方便获取数据。
- 精选图片集：展示我精选的好看的图片。
- 音乐箱：后台持续播放音乐，也提供白噪音功能。
- 笔记：内置一个小notion，可以写笔记，日记。
- 任务箱：目标追踪，快速待办等任务型功能。
后续也将继续新增更多方便的小工具。

# 项目架构
使用Electron是为了能够同时支持网站和桌面端应用的使用，既然有网站了所以也会专门为手机端做响应式界面，但不会特别为手机端做应用适配，以降低初期的维护成本。

# 项目风格
现代化工具软件常见的左侧导航 + 主内容区布局，配色和样式全局统一，极简干净的UI设计，使用流畅。

# UI 开发约定
- 新增 UI 优先使用 `src/components/ui` 中的 shadcn/ui 组件，需要新基础组件时通过 `npx shadcn@latest add <component>` 添加。
- 弹窗、确认、抽屉、下拉选择、页签、Tooltip 等标准交互不要重新实现，也不要使用浏览器原生 `alert` / `confirm` / `prompt`。
- 业务级复合交互可以封装在 `src/components` 中，底层仍应组合 shadcn/ui 原语。
- 保留丝月工坊的现有品牌视觉；主题 token 统一在 `src/styles.css` 管理，同时验证浅色、深色与 Electron 环境。

